# olympus-bug-screenshots

This **public** repository stores image files committed by Olympus (portal bug reports) so `raw.githubusercontent.com` URLs work inside GitHub Issues when the main codebase repo is private.

Paths under `.github/bug-report-assets/` are created automatically by the Olympus Core GitHub integration.
